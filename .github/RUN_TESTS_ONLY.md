# GitHub工作流 - 只运行测试（快速参考）

## ✅ 当前配置

工作流已配置支持通过commit消息跳过二进制构建。

## 🚀 使用方法

### 只运行测试（跳过二进制构建）

```bash
# 方式1：在新的commit中添加标记
git commit -m "test: your message [skip-binaries]"
git push

# 方式2：修改最后的commit（如果还没push）
git commit --amend -m "test: your message [skip-binaries]"
git push -f  # 注意：如果已经push过，需要force push

# 方式3：同时跳过二进制和发布
git commit -m "test: your message [skip-build]"
git push
```

### 当前commit状态

✅ 已修改为包含 `[skip-binaries]` 标记：
```
fix(test): resolve CI test failures by using range checks [skip-binaries]
```

### 推送测试

```bash
# 直接推送，将只运行测试，跳过二进制构建
git push
```

## 📊 工作流执行对比

| Commit消息 | build | build-binaries | tests | release | 总时间 ⏱️ |
|-----------|-------|----------------|-------|---------|----------|
| 普通消息 | ✅ | ✅ | ✅ | ✅* | ~2小时 |
| `[skip-binaries]` | ✅ | ❌ | ✅ | ❌ | ~10分钟 ⭐ |
| `[skip ci]` | ❌ | ❌ | ❌ | ❌ | 0分钟 |

\* release只在main/beta分支且有新版本时运行

## 🎯 当前场景

你正在测试CI修复，推荐使用 `[skip-binaries]`：
- ✅ 大幅节省时间（从2小时降到10分钟）
- ✅ 依然运行所有测试
- ✅ 跳过耗时的二进制编译

## 💡 其他选项

### 手动触发（GitHub Web界面）

1. 访问: `https://github.com/<你的仓库>/actions/workflows/build.yml`
2. 点击 "Run workflow"
3. 选择分支
4. 点击运行

**注意**: 手动触发会运行完整流程（无法跳过）

### 创建Pull Request

PR会自动触发测试，但也会运行完整流程。

## 📝 标记说明

| 标记 | 效果 |
|------|------|
| `[skip-binaries]` | 跳过二进制构建，运行测试 ⭐ |
| `[skip-build]` | 同上 |
| `[skip ci]` | 完全跳过CI（GitHub默认） |
| `[ci skip]` | 完全跳过CI（GitHub默认） |

## ⚡ 快速命令

```bash
# 当前情况：推送测试修复
git push

# 如果需要修改消息
git commit --amend
# 然后在编辑器中添加 [skip-binaries]
git push -f

# 查看CI状态
git push && \
echo "查看运行状态: https://github.com/$(git remote get-url origin | sed 's/.*://;s/.git$//')/actions"
```

---

**下一步**: 直接运行 `git push` 来触发只运行测试的CI！
